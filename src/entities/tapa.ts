export type ProtoTapa = {
    name?: string;
    origin?: string;
    ingredient?: string;
    tasted?: boolean;
};

export type Tapa = {
    id: string;
    name: string;
    origin: string;
    ingredient: string;
    tasted: boolean;
};
