export type ProtoTapa = {
    name?: string;
    origin?: string;
    ingredient?: string;
    tasted?: boolean;
};

export type Tapa = {
    id: number;
    name: string;
    origin: string;
    ingredient: string;
    tasted: boolean;
};
